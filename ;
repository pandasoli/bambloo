<script lang='ts'>
	import { ui } from '@/stores/ui.ts'

	import { AppTabsList } from '@/models/AppTab.ts'

	import Button from '@/components/Button.svelte'
</script>

<main>
	{#each AppTabsList as tab}
		<Button class='smth' type='blue' inactive={$ui.tab !== tab} onclick={() => ui.setTab(tab)}>{tab}</Button>
	{/each}
</main>

<style>
	main {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 8px;
		padding-block: 7px
	}

	main > * {
		height: 20px;
		width: 70px;
		font-size: 10px;
		text-transform: capitalize
	}
</style>
